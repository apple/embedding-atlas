# viewer

The frontend application for the embedding atlas.

Build:

```bash
npm run build
```

Start development server:

```bash
npm run dev
```

You can then go to http://localhost:5173/#/test to view a test dataset.
